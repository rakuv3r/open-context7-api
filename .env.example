# =============================================================================
# open-context7 Setup
# =============================================================================
# Copy this file to .env.dev/.env.beta/.env.prod and add your real values

# =============================================================================
# AI SERVICE (REQUIRED)
# =============================================================================
# Supports 100+ AI providers through LiteLLM
# Provider list: https://docs.litellm.ai/docs/providers
# Model formats: https://docs.litellm.ai/docs/completion/input
# Embedding models: https://docs.litellm.ai/docs/embedding/supported_embedding

# Chat service for document processing
CHAT_API_KEY=your_chat_api_key_here
CHAT_BASE_URL=https://api.openai.com/v1
CHAT_MODEL=gpt-4o

# Embedding service for vector generation
EMBEDDING_API_KEY=your_embedding_api_key_here
EMBEDDING_BASE_URL=https://api.openai.com/v1
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_DIMENSION=1536

# =============================================================================
# DATABASE
# =============================================================================
# Qdrant for document storage
# Local: http://localhost:6333
# Cloud: https://your-cluster.qdrant.io:6333
QDRANT_URL=http://localhost:6333

# =============================================================================
# TASK SETUP
# =============================================================================
# Git clone timeout in seconds (300 = 5 minutes)
# Use bigger value for large repos with slow internet
GIT_CLONE_TIMEOUT=300

# Default Git branch name for GitLab repositories
# Most older repos use 'master', newer ones use 'main'
GIT_DEFAULT_BRANCH=master

# =============================================================================
# LOGGING
# =============================================================================
# How much detail to show in logs
# Options: TRACE, DEBUG, INFO, SUCCESS, WARNING, ERROR, CRITICAL
# Use DEBUG for testing, INFO for production
LOG_LEVEL=INFO

# Folder for log files
LOG_PATH=logs

# Main log file name
LOG_FILENAME=open-context7.log

# When to create a new log file (500 MB recommended)
LOG_ROTATION="500 MB"

# How long to keep old log files (12 months recommended)
LOG_RETENTION="12 months"

# =============================================================================
# MONITORING (Optional)
# =============================================================================
# Sentry error tracking (optional - delete these lines if not using)
# Get key from: https://sentry.io/settings/projects/your-project/keys/
SENTRY_DSN=

# How many errors to report (0.1 = 10% of errors)
# Range: 0.0 to 1.0
SENTRY_SAMPLE_RATE=0.1

# How many requests to track (0.05 = 5% of requests)
# Range: 0.0 to 1.0 (use small number to save usage)
SENTRY_TRACES_SAMPLE_RATE=0.05
